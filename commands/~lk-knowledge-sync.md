---
description: Sync shared knowledge markdown files into the local DB
allowed-tools: Bash(lk *)
---

Sync .knowledge/ markdown files with the local SQLite database.

## Procedure
1. Run: `lk sync --json`
2. Display sync results (added/updated/removed/unchanged counts)
3. If there were updates, briefly summarize what changed
